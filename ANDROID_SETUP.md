## Android setup

This repository contains the Flutter/Dart source and an AndroidManifest customization.
If you create the platform scaffolding locally, run:

    flutter create .

Then keep `lib/`, `pubspec.yaml`, and the AndroidManifest changes from this package.
